<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
		"-//mybatis.org//DTD Mapper 3.0//EN" 
		"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
		
<mapper namespace="com.itbank.repository.CampingDAO">

	<select id="selectAll" resultType="camping">
		select * from camping order by camping_idx
	</select>
	
	<select id="selectOne" parameterType="int" resultType="camping">
	 	select *
    		from camping 
    		natural join camping_activity
    		natural join camping_internal
    		natural join camping_introduce 
    		natural join camping_safety_device 
    		where camping_idx = #{camping_idx}
	</select>
	
</mapper>